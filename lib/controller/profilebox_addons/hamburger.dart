import 'package:flutter/material.dart';

class ProfileHamburger extends StatelessWidget {
  const ProfileHamburger({super.key});

  @override
  Widget build(BuildContext context) => IconButton(
        onPressed: () {},
        icon: const Icon(Icons.menu),
        color: Colors.black,
      );
}
